# ClearX 工程协定

## 产品范围

ClearX 是一款只读的 macOS 磁盘空间分析工具，交互模型参考 OmniDiskSweeper。MVP 允许用户选择本地卷或文件夹，扫描后展示文件与文件夹的聚合大小。与 OmniDiskSweeper 不同，ClearX 会持久化扫描快照，并用其加速后续扫描。

MVP 不删除、移动、修改、在 Finder 中显示或以其他方式操作被扫描文件；不请求“完全磁盘访问权限”，也不实现特权扫描。

## 平台与技术

- 应用名称：`ClearX`
- Bundle 标识：`com.hang.clearx`
- 语言：仅 Swift
- 界面：SwiftUI
- 并发：Swift Concurrency（`async`/`await`、`Task`；需要隔离时使用 actor）
- 最低部署目标：macOS 14
- 依赖：仅使用 Apple 平台框架和 `GRDB.swift`；后者通过 Swift Package Manager 引入，未经更新本协定不得添加其他包。
- 打包：标准 Xcode macOS App 工程；MVP 开发期间采用本地签名运行，暂不启用 App Sandbox。

## 用户流程

1. 启动后进入扫描源选择页，显示一个“本机磁盘”入口、可写外接本地卷和文件夹选择器；默认隐藏 APFS 辅助卷、只读系统卷、磁盘映像和网络卷。
2. 每个选中的扫描源都在独立结果窗口中打开。
3. 结果使用 Finder 风格的多列目录浏览器，遵循 OmniDiskSweeper 的交互模型。
4. 选中目录后，在下一列显示其子项；每列均按计算出的大小降序排列。
5. 每行显示名称、格式化大小及其占父目录的比例。
6. 扫描时持续发布部分结果，不阻塞界面。
7. 支持取消；取消后保留部分结果，并标记为不完整。
8. 结果窗口打开期间监听文件系统事件，短暂合并事件后仅刷新受影响的目录分支。
9. 再次打开已有缓存的扫描源时，立即显示上次结果并在后台同步。正常同步仅在工具栏显示低干扰指示；仅在数据不完整、权限不可用、事件历史丢失或失败时提升为明确状态。
10. 工具栏提供“重新完整扫描”，为当前扫描源构建新快照；替换快照原子提交前持续显示旧结果。

## 扫描语义与索引

- 包含隐藏文件，并遍历包目录内容。
- 不跟随符号链接。
- 不跨越选定根目录之外的已挂载卷边界。
- 跳过不可读项目，保留可访问部分的聚合结果，并在界面标记结果不完整。
- 目录大小等于其已扫描后代的大小总和。
- 同一扫描内的硬链接按临时文件系统对象身份仅计入一次已分配大小；后续链接保留在树中并标记为不额外占用。此规则不适用于 APFS 克隆文件。
- 对可能共享内容块的 APFS 文件，报告系统提供的已分配大小；不尝试在多个路径间分配共享块，并标记其可能不同于实际可释放物理空间。
- 文件系统访问与大小聚合不得运行在主 actor 上。
- 使用单个全局、有上限的扫描调度器，所有扫描源共用最多 4 个元数据扫描任务；SQLite 快照写入走受控的事务路径。多个窗口同时扫描时，当前聚焦窗口优先获得任务。
- 每次完整或部分扫描均持久化为紧凑层级：节点保存父节点 ID 和本地名称，不重复保存完整相对路径；同时保存类型、已分配大小、逻辑大小，以及可取得的修改日期。
- 已取消或不完整的快照可用于即时展示，但不得作为事件增量更新的基线；下次打开时应自动对其扫描根目录执行完整重扫。
- 后续启动时，利用每卷持久化的文件系统事件进度定位变更分支，仅重扫这些分支。
- 每个扫描源与卷独立保存最近一次成功处理的事件进度。
- 扫描或分支刷新前先建立设备级事件流；在固定事件高水位之前重放、归一化并处理事件，再在同一数据库事务中提交节点变更与该检查点。高水位之后的事件保持待处理状态。
- 仅在设备级事件重放成功完成、事件路径仍位于预期卷和扫描根目录内且未出现事件丢失或含糊标志时，才可采用增量结果。事件 ID 的正常数值跳跃不可视为丢失；仅 ID 重置或回绕、检查点或卷 UUID 不匹配、`RootChanged`、`UserDropped`、`KernelDropped`、`MustScanSubDirs` 或流失败才要求完整重扫。
- 文件、删除项目或不存在目录的事件路径，必须向上归一化到扫描根目录内最近的可访问父目录后再去重和重扫；无法归一化时完整重扫。
- 仅当事件检查点证明扫描源自上次完整快照后没有变化时，才直接采用缓存；已知变化仅重扫相关分支；无法证明有效性时执行完整扫描。修改日期仅用于分支刷新时的细粒度比对与诊断，不作为全局有效性证明。
- 持久化快照索引使用 GRDB 与 SQLite WAL 模式，采用显式 Schema 迁移、已索引的层级查询及批量事务写入。
- 刷新写入必须使用事务：失败时保留最后一份已提交快照。启动时若数据库无法打开或校验失败，应隔离该数据库并自动重建受影响扫描源，不得阻塞新的全量扫描。
- 已验证刷新时，自动移除不存在的快照节点；不提供逐文件或逐目录的缓存删除控件。
- 无法访问的扫描根目录应视为“不可用”而非“已删除”，保留上次快照直至 ClearX 再次验证根目录。
- 持久化快照的缓存预算为 1 GB。超出预算时，自动淘汰最久未使用且当前不可访问扫描源的完整快照，并在源列表明确标记该源缓存已清理。
- 若缓存已超过 1 GB 且没有可安全淘汰的不可访问扫描源，标记“缓存空间不足”并停止持久化新的候选快照；不得静默删除可访问扫描源的缓存。
- 快照仅保存在 ClearX 本地 `Application Support` 目录，不参与云同步或备份。MVP 依赖 macOS 用户帐户与 FileVault，不实施数据库级加密。
- 文件系统事件历史只是优化手段，不是正确性证明：事件历史丢失、根目录或卷身份改变、元数据不可用或事件含义不明确时，必须安全地重扫受影响扫描源。

## 工程规则

- 除非用户明确要求更新代码、设计稿、PRD 或指定文件，否则默认仅进行对话讨论，不修改文件。讨论必须基于产品目标、工程约束与设计原则提出专业判断、风险和替代方案，不得无依据附和。
- 所有修改都必须有明确证据来源：原型 HTML/CSS、需求文档、技术方案、现有源码模式、构建或运行日志，或用户的明确指令。证据不足时先查证再修改；不得凭猜测、记忆或主观印象直接改代码。
- 文档按职责归位：项目说明和文档索引写入 `README.md`；代码结构与 AI 定位说明写入 `CODE_STRUCTURE.md`；产品定位、流程和一级页面目录写入 `docs/PRODUCT_REQUIREMENTS.md`；本期与非本期范围、后续版本和实施里程碑写入 `docs/ROADMAP.md`；侧边栏一级及二级页面需求写入 `docs/prd/` 下对应文件；路径规则、风险和 Provider 写入 `docs/CLEANUP_SCOPE_DESIGN.md`；架构、执行器、权限、性能和测试写入 `docs/TECHNICAL_DESIGN.md`。
- 当 SwiftUI 工程的目录结构、文件职责、页面路由、主要类型、状态流、服务层、组件拆分或测试结构发生变化时，必须同步更新 `CODE_STRUCTURE.md`；无关改动不得机械更新该文档。
- 扫描领域逻辑必须独立于 SwiftUI，以便无需渲染视图即可测试。
- 显式建模扫描状态：空闲、扫描中、已完成、已取消、以及不完整或受权限限制。
- 排序必须确定：先按大小降序，大小相同时按本地化名称升序。
- MVP 固定使用大小排序，不提供按名称、修改日期或路径切换排序的控件。
- 用户可见的文件大小使用 `ByteCountFormatter` 或等效的 Apple API；不得手写字节单位格式化。
- 公共类型和错误以 `URL` 设计；不得在应用领域层传递原始文件系统路径字符串。
- 仅数据库内部可使用名称和父节点 ID 等紧凑字符串；扫描源根目录、事件与诊断的公共值必须使用 `URL`。
- 按可见浏览器列惰性查询持久化层级，并进行有限行预取；不得在 SwiftUI 状态中实体化整个扫描源树。
- 对不明显的文件系统或并发决策，在代码中添加简洁说明。

## 质量门槛

- 为聚合、排序、符号链接排除、取消和不可读项目处理添加单元测试。
- 在可行处为扫描状态展示添加聚焦的 UI 或模型测试。
- 测试事件重放完成、检查点与节点原子提交、扫描期间变更、分支刷新期间变更、事件丢失或含糊标志、事件路径越界和删除路径归一化。
- 维护可重复的本地基准目录，并满足：缓存的 100 万节点结果在 1 秒内展示首列；无变更的重复打开在 3 秒内完成验证；1 万项变更仅刷新受影响分支，并在 5 秒内完成提交。
- 记录首次全量扫描的吞吐与峰值内存；不设固定耗时门槛，因为存储介质、权限和目录深度存在差异。
- 每次变更完成前均须构建并运行相关测试套件。
- 未更新本协定不得扩展 MVP 范围。

## 延后决策

- TODO：后续引入安全清理功能时，默认仅勾选低风险项；高风险项只展示或要求二次确认。每个清理项必须解释其性质、空间来源、可清理理由及清理影响。所有处理仅在本机进行，不上传路径、文件名、依赖列表、项目结构或清理记录；优先调用官方工具；开发者模式不得读取源码内容，仅统计依赖目录、lockfile 状态、缓存和构建产物。
- TODO：对外发布前，决定 ClearX 是否面向 Mac App Store、Developer ID 分发或两者兼顾；随后设计相应的沙盒 entitlement 与文件访问权限模型。
- TODO：发布前，添加并验证访问文件修改日期所需的隐私清单声明。

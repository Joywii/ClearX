# ClearX 产品需求

## 定位

ClearX 是面向 macOS 的只读磁盘空间分析工具。用户选择本机磁盘、外接本地卷或任意文件夹后，通过 Finder 风格的多列浏览器定位大文件和目录。

## 核心流程

1. 选择扫描源。
2. 在独立结果窗口中浏览按大小排序的目录列。
3. 首次扫描显示渐进结果；同一应用会话内再次打开时先展示完整内存缓存。
4. 应用退出后不保留缓存；设备级事件校验和跨启动缓存复用属于后续版本。

## 页面目录

- 扫描源选择页。
- 扫描结果窗口。

MVP 不提供删除、移动、Finder 跳转、网络卷或完全磁盘访问。
